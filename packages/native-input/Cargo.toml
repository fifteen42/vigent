[package]
name = "vigent-native-input"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", features = ["napi9"] }
napi-derive = "2"
enigo = { version = "0.3", features = ["serde"] }

[build-dependencies]
napi-build = "2"
